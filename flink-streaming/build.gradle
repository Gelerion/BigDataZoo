group 'com.gelerion.flink.streaming'

apply plugin: 'scala'

dependencies {
    compile libraries.flink
    compile libraries.flinkJava
    compile libraries.flinkStreamingJava

    compile libraries.flinkScala
    compile libraries.flinkStreamingScala
}